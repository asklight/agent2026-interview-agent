name: agent2026-interview-agent

services:
  mysql:
    image: mysql:8.0
    container_name: agent2026-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-agent2026_interview_agent}
      MYSQL_USER: ${MYSQL_USERNAME:-agent2026_user}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?MYSQL_PASSWORD is required}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?MYSQL_ROOT_PASSWORD is required}
      TZ: Asia/Shanghai
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --default-time-zone=+08:00
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - agent2026

  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    image: agent2026-interview-agent-server:latest
    container_name: agent2026-server
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: prod
      MYSQL_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE:-agent2026_interview_agent}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
      MYSQL_USERNAME: ${MYSQL_USERNAME:-agent2026_user}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?MYSQL_PASSWORD is required}
      RESOURCE_TOKEN_SECRET: ${RESOURCE_TOKEN_SECRET:?RESOURCE_TOKEN_SECRET is required}
      TJU_LLM_API_URL: ${TJU_LLM_API_URL:?TJU_LLM_API_URL is required}
      TJU_LLM_API_KEY: ${TJU_LLM_API_KEY:?TJU_LLM_API_KEY is required}
      TJU_LLM_MODEL: ${TJU_LLM_MODEL:-tju-llm}
      TJU_LLM_PROXY_HOST: ${TJU_LLM_PROXY_HOST:-}
      TJU_LLM_PROXY_PORT: ${TJU_LLM_PROXY_PORT:-0}
      TZ: Asia/Shanghai
      JAVA_OPTS: ${JAVA_OPTS:-}
    expose:
      - "8080"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS --max-time 5 http://127.0.0.1:8080/api/health/db >/dev/null || exit 1"]
      interval: 15s
      timeout: 6s
      retries: 12
      start_period: 60s
    networks:
      - agent2026

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    image: agent2026-interview-agent-web:latest
    container_name: agent2026-web
    restart: unless-stopped
    depends_on:
      server:
        condition: service_healthy
    ports:
      - "${WEB_PORT:-80}:80"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- -T 5 http://127.0.0.1/ >/dev/null || exit 1"]
      interval: 15s
      timeout: 6s
      retries: 8
      start_period: 15s
    networks:
      - agent2026

volumes:
  mysql-data:

networks:
  agent2026:
    driver: bridge
